import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>What do you need to do to consider the day done?</h1>
        </header>
    );
};

export default Header;